import { AlignCenterDirective } from './align-center.directive';

describe('AlignCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new AlignCenterDirective();
    expect(directive).toBeTruthy();
  });
});
